// Define what properties a Group has based on your application's needs
interface GroupType {
    id: string; // Assuming every group has an ID
    name: string; // The name of the group
    // Add any other properties that a group might have
  }
  